# STUDENT_APP_INTEGRATION_PLAN

**الحالة:** تصوّر تصميمي (Design Only) — لا تنفيذ في هذه المرحلة، كما هو مطلوب صراحة.
**المرجع:** يُبنى هذا المستند على تحقق كودي فعلي في `apps/api/src`، موثَّق في `ARCHITECTURE_BOUNDARY_DOCUMENT.md`.

---

## 1. مسؤوليات DentPilot Student App

بحسب ما يُظهره الكود الحالي (دور `STUDENT_INTEGRATION` بصلاحيتين فقط: `catalogs:read`, `files:access`)، يجب أن يبقى Student App مسؤولًا حصريًا عن:

- تجربة الطالب: عرض الكتالوجات المتاحة له (`/catalog/*`)، حالة مسوّداته، وسجل تقديماته.
- إعداد الحالة السريرية (Case Preparation) — تحرير `payload` المسودة محليًا قبل الإرسال.
- التقاط المرفقات ورفعها عبر تدفق presigned URL الموجود فعليًا.
- تفاعل الإرسال (submission) عبر `POST /api/v1/student/submissions` — منفَّذ ويعمل اليوم بلا تعديل.
- تجربة استخدام offline محدودة (تخزين محلي مؤقت لمسودة قيد التحرير فقط، وليس كمصدر رسمي بديل — يخالف ذلك قيود البرومبت صراحة).
- التواصل مع University Core **حصريًا عبر API** — لا اتصال مباشر بقاعدة البيانات، ولا نسخ من business logic (`packages/domain` غير مستهدف للاستهلاك من Student App، تمامًا كما لا يستهلكه `apps/web` اليوم).

Student App **لا** يملك: قرار الموافقة/الرفض، الدرجات، سياسات التقييم، بيانات طلاب آخرين، أو أي عملية إدارية — وهذا مؤكَّد كوديًا وليس افتراضًا تصميميًا (`rolePermissions.STUDENT_INTEGRATION`).

---

## 2. نقاط التكامل المطلوبة — حالة كل واحدة اليوم

| نقطة التكامل | الحالة الفعلية في الكود | الإجراء المطلوب |
|---|---|---|
| تسجيل دخول الطالب | `POST /api/v1/auth/login` يعمل لأي دور بما فيه `STUDENT_INTEGRATION` — **لكن لا مسار لإصدار حساب STUDENT_INTEGRATION مربوط بـ `student_id`** | إغلاق فجوة التزويد (§4) قبل أي استخدام فعلي |
| قراءة الكتالوجات (أقسام/سنوات/مستويات/أفواج) | منفَّذ بالكامل، ومسموح لـ `STUDENT_INTEGRATION` عبر `catalogs:read` | لا شيء إضافي |
| إنشاء/تعديل مسودة (`student_drafts`) | **غير منفَّذ.** الجدول موجود، `submitDraft` يقرأ منه، لكن لا `POST/PUT/GET /api/v1/student/drafts` | **أولوية أولى** — أهم فجوة تحجب أي استخدام فعلي لـ Student App |
| إرسال المسودة (submit) | منفَّذ ويعمل: يحوّل draft إلى `submission_snapshot` غير قابل للتعديل، مع `sequence` تصاعدي وربط `resubmission_of_snapshot_id` | لا شيء إضافي بنيويًا |
| رفع مرفق (presign/complete/link/read) | منفَّذ بالكامل مع تحقق ملكية صارم | لا شيء إضافي بنيويًا |
| قراءة حالة التقديم ونتيجته المنشورة | **غير منفَّذ للطالب.** `GET /api/v1/staff/submissions/:id` مقيَّد بصلاحية `cases:review` (موظفين فقط) | يلزم إضافة مسار قراءة مخصَّص للطالب المالك، يُخفي حقول داخلية (ملاحظات المشرف غير المنشورة، السياسات غير المنشورة، بيانات تحكم مؤسسية) — وهذا التصنيف موثَّق مسبقًا في `docs/architecture/prototype-behavior-inventory.md:45` كنية تصميمية واضحة |
| إعادة الإرسال بعد طلب تعديل (resubmission) | جزئي: العمود `resubmission_of_snapshot_id` موجود في المخطط، لكن لا مسار API يستهلكه بعد لإنشاء مسودة جديدة من طلب تعديل | يُبنى فوق حل الفجوة الأولى (draft CRUD) |
| إشعارات (تعديل مطلوب / تم النشر / درجة جديدة) | البنية التحتية جاهزة (`outbox_events` يُكتب مع كل mutation ضمن نفس transaction)، لكن **لا مستهلك (consumer/publisher) بعد** — موثَّق ذاتيًا في ADR 007 كحد Phase 1 مقصود | مستقبلي — لا يحجب الإطلاق الأول (يمكن الاعتماد على polling دوري من التطبيق مؤقتًا) |

---

## 3. تصميم تدفق المصادقة (Authentication Flow)

**لا تغيير على نموذج مصادقة البوابة الحالي.** الخيار المطلوب هو قرار إضافي محصور بعميل Student App فقط:

- **الوضع الحالي:** `CORS_ORIGIN` قيمة واحدة (`z.string().url()` في `packages/config`)، وcookies بـ `SameSite=strict`. هذا مناسب تمامًا لعميل متصفح واحد (البوابة) لكنه لا يفترض عميلًا ثانيًا صراحة.
- **الخيارات المتاحة لعميل Student App (بلا كسر نموذج البوابة):**
  1. **إن كان Student App تطبيق ويب/PWA بمصدر منفصل:** تحويل `CORS_ORIGIN` إلى قائمة مصادر مسموحة (allow-list) بدلًا من قيمة واحدة — تعديل صغير في `packages/config` وتسجيل CORS، لا يمس نموذج الجلسة أو RLS.
  2. **إن كان تطبيقًا أصليًا (native mobile):** الاعتماد على نفس آلية الجلسة الحالية (تخزين cookie/token في secure storage محلي بدل متصفح) هو الأنسب لأنه **لا يضيف نموذج مصادقة ثانٍ** إلى النظام — يبقي `AuthService` مصدرًا وحيدًا لإصدار/إبطال الجلسات لكل الأدوار. يُنصح بتفادي إدخال JWT أو API Key منفصلة إلا إذا وُجد سبب تشغيلي واضح (مثل SSO مؤسسي)، لأن ذلك يضاعف سطح المصادقة بلا داعٍ معماري ظاهر اليوم.
- **القرار مطلوب قبل التنفيذ، وليس تعديلًا معماريًا جوهريًا** — لأن كليهما يعملان فوق `AuthService.authenticate()` نفسه بلا تعديل على منطقه.

---

## 4. فجوة التزويد (Provisioning) — إغلاقها شرط مسبق

`redeemInvitation()` الحالي (`apps/api/src/security/auth.ts:63`) ينشئ حسابًا بلا `student_id` مهما كان الدور المطلوب في الدعوة. لتفعيل Student App فعليًا:

1. يجب أن يدعم مسار الدعوة (أو workflow إداري منفصل صريح) تمرير `studentId` عند إصدار دعوة بدور `STUDENT_INTEGRATION`، والتحقق أن السجل ينتمي لنفس المؤسسة قبل الحفظ.
2. هذا يتوافق تمامًا مع ما هو موثَّق بالفعل في `docs/operations/known-limitations.md` البند 4 — أي أن الفريق يعرف هذه الفجوة، وهي ليست انحرافًا غير متوقَّع.
3. لا حاجة لتغيير مخطط القاعدة (`accounts.student_id` موجود مسبقًا) — التعديل يقتصر على منطق `AuthService`/مسار الدعوة.

---

## 5. تدفق الإرسال (Submission Lifecycle) — لا تغيير مطلوب

مسار draft → submit → snapshot غير قابل للتعديل مصمَّم بالفعل بشكل صحيح ويُطبَّق اليوم:
- تحقق ملكية صارم (`principal.studentId === draft.student_id`).
- immutability بعد الإرسال (ADR 006) — لا Student App ولا حتى Staff يمكنهم تعديل snapshot بعد إنشائه؛ فقط قرارات لاحقة (revision request, approve, grade) تُضاف كسجلات جديدة مرتبطة به.
- `idempotencyKey` إجباري على كل عملية إرسال/ربط — يحمي من إعادة إرسال مضاعف نتيجة انقطاع شبكة جوّال، وهو سيناريو متوقَّع لتطبيق طالب.

**لا توصية بأي تعديل هنا** — هذا الجزء جاهز للاستهلاك من Student App بمجرد حل فجوة draft CRUD (§2).

---

## 6. اعتبارات Offline

القيود الصريحة في البرومبت تمنع استخدام Local Storage كبديل للبيانات المركزية أو إنشاء قاعدة بيانات مستقلة داخل Student App للبيانات الرسمية. التصميم المتوافق مع ذلك:

- تخزين محلي **مؤقت** فقط لمسودة قيد التحرير (نفس شكل `payload` الذي سيُرسل لاحقًا)، مع `idempotencyKey` يُولَّد محليًا عند بدء التعديل ويُستخدم عند الاتصال لاحقًا.
- عند استعادة الاتصال: مزامنة المسودة إلى `student_drafts` عبر endpoint الإنشاء/التعديل الجديد (§2)، ثم استخدام نفس `idempotencyKey` عند الإرسال لتفادي التكرار.
- **لا** تخزين نسخة من الحالة الرسمية (snapshots, grades, decisions) محليًا كمصدر يُعتمَد عليه دون تحديث من الخادم — يُعرض فقط كآخر ما وصل من الخادم، مع مؤشر "قد لا يكون محدَّثًا" عند انقطاع الاتصال.

---

## 7. معالجة التعارض (Conflict Handling)

النظام يوفر أساسًا جاهزًا:
- `student_drafts.revision` (عمود موجود في المخطط) يمكن استخدامه لقفل تفاؤلي (optimistic lock) بنفس نمط `assertOptimisticLock` المستخدم في وحدات أخرى (enrollments, term results) — يُطبَّق عند بناء endpoint التعديل.
- بعد الإرسال، لا تعارض ممكن أصلًا لأن السجل غير قابل للتعديل — أي تعديل لاحق (طلب تعديل) يُنشئ دورة submit جديدة (`resubmission_of_snapshot_id`)، لا تعديلًا في مكانه.

---

## 8. دورة حياة رفع المرفقات

جاهزة بالكامل اليوم (`presign-upload → complete → link → presign-read`)، بما في ذلك تحقق sha256/type/size والربط الحصري بمسودة يملكها نفس principal. **لا تعديل مطلوب.** يُنصح فقط أن يلتزم Student App بإعادة استخدام idempotency key عند إعادة محاولة رفع فشل جزئيًا بسبب الشبكة.

---

## 9. إمكانيات الإشعارات

`outbox_events` يُكتب اليوم مع كل mutation مهم (تسجيل دخول، تقديم، قرار سريري، درجة) ضمن نفس transaction — جاهز لبناء ناشر أحداث لاحقًا (webhook/push) بلا إعادة تصميم النموذج. حتى بناء ذلك الناشر، يمكن لـ Student App الاعتماد على polling دوري لحالة تقديماته (بعد إضافة مسار القراءة المخصص في §2) كحل مؤقت غير معماري.

---

## 10. نهج المزامنة المستقبلي

لا حاجة لطبقة مزامنة معقدة: النموذج الحالي (API + idempotency + immutable snapshots) كافٍ لعميل "online-first مع تسامح انقطاع قصير". لا يُنصح ببناء طبقة sync ثنائية الاتجاه كاملة (bi-directional sync engine) لأنها تُدخل تعقيدًا غير مبرَّر بالنظر إلى أن كل كتابة رسمية تمر أصلًا عبر endpoint واحد قابل لإعادة المحاولة الآمنة.

---

## ملخص التوصيات مرتبة حسب الأولوية

1. **إلزامي قبل أي ربط فعلي:** بناء `POST/PUT/GET /api/v1/student/drafts[/:id]` بنفس نمط ownership checks الموجود في `submitDraft`/`linkToDraft`.
2. **إلزامي قبل أي ربط فعلي:** إغلاق فجوة تزويد `accounts.student_id` في مسار الدعوة.
3. **قرار تصميمي مطلوب قبل التنفيذ (ليس تعديلًا بنيويًا):** آلية مصادقة/CORS لعميل Student App (§3).
4. **مستحسن قبل الإطلاق:** مسار قراءة حالة تقديم مخصَّص للطالب (يُخفي الحقول الداخلية) بدل الاعتماد على مسار الموظفين.
5. **مستقبلي وليس حاجزًا:** ناشر أحداث outbox للإشعارات الفورية.

---

## القرار المعماري النهائي

**(A) — النظام الحالي مطابق للهدف المعماري ويمكن البناء فوقه، بشرط إغلاق فجوتين محددتين قبل أي ربط فعلي مع Student App.**

المسوّغات المبنية على الكود الفعلي لا على الافتراض:

- الحدود بين التطبيقات (Student App مقابل University Core) **مطبَّقة فعليًا اليوم**، لا مخطَّطة فقط: دور `STUDENT_INTEGRATION` محدود الصلاحيات موجود في نموذج الهوية والتفويض، مع تحقق ملكية صريح على كل عملية طلابية (submit, link attachment)، وaudit trail كامل.
- ملكية البيانات مركزية بالكامل في PostgreSQL خلف RLS إجباري على مستوى القاعدة — لا مسار كتابة بديل ممكن حتى لو حاول عميل مستقبلي كسره.
- Business logic (`packages/domain`) لا يتسرب إلى أي عميل — البوابة الحالية نفسها لا تستهلكه، وهذا يثبت أن الفصل مُطبَّق عمليًا وليس قاعدة على الورق فقط.
- الفصل بين "مسودة قابلة للتعديل" و"سجل رسمي غير قابل للتعديل" (draft → immutable snapshot) هو بالضبط النموذج الذي يحتاجه Student App مستقل مع University Core كمصدر الحقيقة الوحيد.
- الفجوتان الوحيدتان الحقيقيتان (لا endpoint لإنشاء draft، لا ربط student_id عند التزويد) **موثَّقتان ذاتيًا في تقارير الفريق نفسه** كوظائف لاحقة مخطَّطة — وليستا انحرافًا مكتشفًا حديثًا. إغلاقهما لا يتطلب تعديل حدود، أدوار، مخطط قاعدة، أو نموذج تفويض — فقط تنفيذ endpoints إضافية ضمن نفس الأنماط الموجودة فعليًا في `students/service.ts` و`cases/service.ts`.

لا يوجد ما يبرر الحكم (B) بمعنى "تعديلات معمارية" لأن لا شيء من الفجوات يمس الحدود أو الملكية أو نموذج التفويض نفسه — وهي أبعد ما تكون عن (C) "انحراف جوهري" لأن كل دليل كودي يشير إلى تصميم مقصود ومتّسق منذ البداية لدعم بالضبط هذا السيناريو.
